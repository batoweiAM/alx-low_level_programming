#include "main.h"
/**
 * print_alphabet_x10 - prints all alphabet in lowercase 10 times.
 * Return: Always 0.
 */
void print_alphabet_x10(void)
{
	char i = 0. j;

	while (i < 10)
	{
		j = 0;
		while (j <= 26)
		{
			_putchar(j = 'a'; j <= 'z';);
			j++;
		}
		_putchar('\n');
		i++;
	}
}
